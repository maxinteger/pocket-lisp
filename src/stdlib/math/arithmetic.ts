import { nativeFn } from 'stdlib/utils'

export const cbrt = nativeFn(Math.cbrt)
export const sqrt = nativeFn(Math.sqrt)

export const exp = nativeFn(Math.exp)
export const pow = nativeFn(Math.pow)

export const log = nativeFn(Math.log)
export const log2 = nativeFn(Math.log2)
export const log10 = nativeFn(Math.log10)
